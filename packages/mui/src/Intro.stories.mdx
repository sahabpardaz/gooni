import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Intro" />

# Get Started

Gooni Library is a reusable *utility* and *component* library to be used by Sahab developers.

## Install

You can install this package using the command below:

`npm install @my-sahab/mui` or `yarn add @my-sahab/mui`